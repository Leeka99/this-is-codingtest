# 1로 만들기
### 난이도 : 중하
### 제한 풀이시간 : 20분
### 나의 풀이시간 : 시간초과

#### key point
- 바텀업 방식으로 사용해서 문제를 해결함
- 현재 값(n)의 최소 연산횟수를 구하기 위해서는 dp 테이블에서 (n-1)값의 저장된 연산횟수와 x로 나누어 떨어진다면 (n / x)에 저장된 값의 최소값을 구한 후 +1 해주어야 한다.
    - x는 5, 3, 2 중 하나이다.