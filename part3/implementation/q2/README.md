# 문자열 재정렬

### 난이도 : 하
### 제한 시간 : 20분
### 나의 풀이 시간 : 9분 44초

---
#### key point
- 입력받은 문자열을 toCharArray()를 이용해서 char[] 배열에 넣어준다.
- Arrays.sort()를 이용해 정렬한다.
- char[index] - 'A' < 0 이라면 정수로 판단하고 누적해준다.
    - 0 -> 48
    - A -> 65
    - a -> 97
- 알파벳을 먼저 출력하고 이후 정수 합을 출력해준다.
